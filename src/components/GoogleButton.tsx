import Button from './button/Button';

const GoogleButton = () => {
  return (
    <Button
      icon='/img/google.png'
      label='Sign in with Google'
      color='outline'
    />
  );
};

export default GoogleButton;
